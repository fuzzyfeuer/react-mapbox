import * as Types from './FormTypes';

/** Action Creators */

// Use a 'payload' field as describe here, to adhere to Flux standard actions:
// https://redux-toolkit.js.org/api/createaction/

/**
 * Updates the value for an individual map field. The value can be of any datatype, such
 * as a string or array.
 */
//export const setFieldValue = (mapId, fieldId, value) => ({
//    type: Types.SET_FIELD_VALUE,
//    payload: {
//        mapId,
//        fieldId,
//        value
//    }
//});

/**
 * Indicates that the map is busy, typically when the Submit button has been pressed.
 */
//export const setIsBusy = (mapId, isBusy) => ({
//    type: Types.SET_IS_BUSY,
//    payload: {
//        isBusy
//    }
//});
